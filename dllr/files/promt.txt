"Write the code in talk for such contracts. start with jetton minter. Provide the exact code snippet of the smart-contract

I want to make a smart-contract on the open network using tolk language. I want to make a stable-coin that would have 16b$ capitalization from the start. It would be minted directly on the user's wallet and funds would be frozen. How would it be frozen. The swap of the token should be conducted if the specific allowed for the swap amount is defined in the token, if it is not allowed, the swap is not conducted. The minter of the token can send transactions to the jetton wallet contract and define the allowance there. The allowance is defined at the minter contract as we as a service can define what amount to release. So we send transactions on the minter contract that sends transaction to the specific jetton minter and set allowance value there. Please make a perfect readme for that extended jetton minter and jetton wallet extended smart-contracts. As well consider that I want the user wallet to be minted automatically on the very first launch of the program for free with jetton wallet already with 1 DLLR on it (with 0 allowance so it is frozen already). If wallet extended smart-contract is needed include it in the Readme as well."